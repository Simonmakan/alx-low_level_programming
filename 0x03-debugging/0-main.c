#include "main.h"
/**
 * main - debugging
 * Return: 0 fir negative and positive
 */
int main(void)
{
int i;
int i;

i = 0;
positive_or_negative(i);
i = 0;
positive_or_negative(i);
return (0);
return (0);
}
