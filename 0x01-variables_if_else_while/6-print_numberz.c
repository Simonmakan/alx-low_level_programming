#include <stdio.h>
/**
 * main - all single digits are printed
 * Return: (0) (project done well no six)
 */
int main(void)
{
	int s;

	s = 0;
	while
		(s < 10) {
			putchar(s + '0');
			s++;
		}
	putchar('\n');
	return (0);
}
