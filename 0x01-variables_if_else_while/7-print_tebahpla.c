#include <stdio.h>
/**
 * main - lowercase alphabet in reverse format
 * Return: (0) (program running well)
 */
int main(void)
{
	char s;

	s = 'z';
	while
		(s >= 'a') {
			putchar(s);
			s--;
		}
	putchar('\n');
	return (0);
}
