#include <stdio.h>
/**
 * main - all possible combinations
 * Return: (0) (project done well)
 */
int main(void)
{
	int s;

	s = 0;
	while (s < 10) {
		putchar(s + '0');
			s++;
		while (s < 9)
		putchar(',');
		putchar(' ');
	}
	putchar('\n');
	return (0);
}
