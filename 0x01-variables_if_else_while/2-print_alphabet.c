#include <stdio.h>
/**
 * main - alphabet game in lowercase
 * Return: (0) (project done)
 */
int main(void)
{
	char s;

	s = 'a';
	while
		(s <= 'z') {
			putchar(s);
			s++;
		}
	putchar('\n');
	return (0);
}
