#include <ctype.h>
#include "main.h"

/**
 * _isdigit - digits checker
 *  * @c: xters
 * Return: 1 (digit) 0 (otherwise)
 */

int _isdigit(int c)
{
int s = isdigit(c);

if (s == 0)
return (s);
else
return (1);
}
