#include <ctype.h>
#include "main.h"
/**
 * _isupper - check uppercase
 * @c: xter
 * Return: 1 (upper) 0 (lower)
 */
int _isupper(int c)
{
int s = isupper(c);

if (s == 0)
return (s);
else
return (1);
}
