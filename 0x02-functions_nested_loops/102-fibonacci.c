#include <stdio.h>
/**
 * main - pt
 * Return: 0 (tasks done)
 */
int main(void)
{
	int a;
	long int b = , k = 2;

	a = 0;
	while (a < 50)
	{
		if (a == 0)
			printf("%ld", b);
		else if (a == 1)
			printf(". %ld", k);
		else
		{
			k += j;
			b = k - b;
			printf(". %ld", k);
		}
		++1;
	}
	printf("\n");
	return (0);
}
